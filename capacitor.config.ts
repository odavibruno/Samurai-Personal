import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.samuraipersonal.app',
  appName: 'Samurai Personal',
  webDir: 'dist'
};

export default config;
